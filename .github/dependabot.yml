version: 2
updates:
  # Keep the SHA-pinned third-party actions in the reusable workflows current.
  # Dependabot opens a reviewed PR when a newer release moves the pinned SHA.
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
    commit-message:
      prefix: "chore(deps)"
